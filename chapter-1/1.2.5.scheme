1.2.5 最大公約数
Greatest Common Devisor(GCD)

rがaをbで割った時の余りである場合にaとbの共通因数は性格にbとrの共通な因数である

GCD(a,b) = GCD(b,r)


GCD(206,40) = GCD(40, 6)
            = GCD(6, 4)
            = GCD(4, 2)
            = GCD(2, 0)

上の例ではGCD(206, 40)をGCD(2, 0)へと縮小している。その答えは2。
任意の2つの正の整数から初めて収縮を繰り返し実行することで、常に最終的には2つ目の数値が0であるペアにすることができる。
その時、GCDの値はもう一つの値。
=> ユークリッドの互除法

(define (gcd a b)
  (print "hello")
  (if (= b 0)
      a
      (gcd b (remainder a b))))

これが反復プロセスを生成し、そのステップ数は与えられた数値の対数で増加する。
