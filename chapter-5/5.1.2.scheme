5.1.2 機械設計における抽象化

GCDマシンをプリミティブな剰余命令を使用することなしに構築したい場合、より単純な命令、例えば引き算を用いてどのように剰余を求めるのかを指定しなければならない。

(define (remainder n d)
  (if (< n d) n
       (remainder (- n d) d)))

従ってGCDマシンのデータパス内の剰余命令を引き算命令と比較テストで置き換えることができる。

(assign t (op rem) (reg a) (reg b))

